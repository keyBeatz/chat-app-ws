<div class="chatConversations">
    <ul n:if="$conversations" n:inner-foreach="$conversations as $conversation">
        <li>
            {$conversation->conversation->lastMessage->text}
        </li>
    </ul>
</div>
