<div class="chatConversations">
    conversation
    <ul n:if="$conversations">
        <li n:foreach="$conversations as $conversation">
            {$conversation->conversation->lastMessage->text}
        </li>
    </ul>
</div>
